import { Box, Typography } from "@mui/material";

function Error() {
  return (
    <Box display={"flex"} justifyContent="center" alignItems={"center"}>
      <Typography variant="h2">404-WRONG PAGE</Typography>
    </Box>
  );
}

export default Error;
